# CI 목표 “main 브랜치가 깨지는 걸 막는다”
# 자동화 범위 (최소)
# Unity 프로젝트 열림
# 컴파일 에러 체크
# 지정 플랫폼 빌드 성공 여부
# ❌ 자동 배포
# ❌ 스토어 업로드
# 🔹 왜 빌드까지만 할까?
# 게임은 배포 비용이 크다
# 실수 한 번이 치명적이다
# QA 과정이 필요하다
# 👉 CI ≠ CD

# CI는 추후 추가 예정
name: unity-ci (disabled)

on: []
jobs: {}
